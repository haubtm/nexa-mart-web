export enum StockTakeStatus {
  PENDING = 'PENDING',
  COMPLETED = 'COMPLETED',
}

export enum PriceStatus {
  CURRENT = 'CURRENT',
  EXPIRED = 'EXPIRED',
  PAUSED = 'PAUSED',
  UPCOMING = 'UPCOMING',
}
