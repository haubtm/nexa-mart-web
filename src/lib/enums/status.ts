export enum StockTakeStatus {
  PENDING = 'PENDING',
  COMPLETED = 'COMPLETED',
}

export enum PriceStatus {
  EXPIRED = 'EXPIRED',
  PAUSED = 'PAUSED',
  ACTIVE = 'ACTIVE',
}

export enum EPromotionStatus {
  ACTIVE = 'ACTIVE',
  EXPIRED = 'EXPIRED',
  PAUSED = 'PAUSED',
  UPCOMING = 'UPCOMING',
  CANCELLED = 'CANCELLED',
}
