import { RefundContainer } from '@/features/main';

const RefundPage = () => {
  return (
    <>
      <title>Danh sách trả hàng</title>
      <RefundContainer />
    </>
  );
};

export default RefundPage;
