import { LoginContainer } from '@/features/auth/containers';

const LoginPage = () => {
  return (
    <>
      <title>Đăng nhập</title>
      <LoginContainer />
    </>
  );
};

export default LoginPage;
